# Renkit 交接入口

正式、可随仓库迁移的完整交接文档位于：

```text
docs/RENKIT_HANDOFF.md
```

新账号或新会话开始工作前，必须依次完整阅读：

1. `AGENTS.md`
2. `docs/RENKIT_HANDOFF.md`
3. `CHANGELOG.md` 顶部最近版本

不要只根据旧聊天记录判断当前版本和发布状态。当前仓库曾出现 GitHub `main` 与 Gitee v2 已部署版本不一致，必须按正式交接文档的“接手第一步”核验后再修改。
