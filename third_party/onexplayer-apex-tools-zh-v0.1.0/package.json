{
  "name": "onexplayer-apex-tools",
  "version": "0.1.0",
  "description": "Decky Loader plugin for OneXPlayer Apex fixes and fan control",
  "type": "module",
  "scripts": {
    "prebuild": "echo \"export const BUILD_ID = \\\"build-$(git rev-parse --short HEAD)\\\";\" > src/build_info.ts && echo \"BUILD_ID = \\\"build-$(git rev-parse --short HEAD)\\\"\" > py_modules/build_info.py",
    "build": "rollup -c",
    "watch": "rollup -c --watch",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write 'src/**/*.{ts,tsx}'",
    "format:check": "prettier --check 'src/**/*.{ts,tsx}'",
    "package": "bun run prebuild && rollup -c && rm -rf 'OneXPlayer Apex Tools' && mkdir -p 'OneXPlayer Apex Tools/dist' 'OneXPlayer Apex Tools/py_modules' 'OneXPlayer Apex Tools/assets' && cp package.json plugin.json main.py 'OneXPlayer Apex Tools/' && cp dist/index.js 'OneXPlayer Apex Tools/dist/' && cp py_modules/*.py 'OneXPlayer Apex Tools/py_modules/' && cp -r py_modules/hhd_patches 'OneXPlayer Apex Tools/py_modules/hhd_patches' && mkdir -p 'OneXPlayer Apex Tools/py_modules/oxpec' && for d in py_modules/oxpec/*/; do [ -f \"${d}oxpec.ko\" ] && cp -r \"$d\" 'OneXPlayer Apex Tools/py_modules/oxpec/' || true; done && cp assets/* 'OneXPlayer Apex Tools/assets/' && rm -f OneXPlayer_Apex_Tools.zip && zip -r OneXPlayer_Apex_Tools.zip 'OneXPlayer Apex Tools' && rm -rf 'OneXPlayer Apex Tools'",
    "deploy": "bun run package && cp OneXPlayer_Apex_Tools.zip ~/Downloads/ && echo 'Copied OneXPlayer_Apex_Tools.zip to ~/Downloads/'",
    "install-plugin": "bun run package && sudo unzip -o OneXPlayer_Apex_Tools.zip -d ~/homebrew/plugins/ && sudo systemctl restart plugin_loader && echo 'Plugin installed and loader restarted'"
  },
  "dependencies": {
    "@decky/api": "^1.1.3",
    "tslib": "^2.6.0"
  },
  "devDependencies": {
    "@decky/rollup": "^1.0.2",
    "@decky/ui": "^4.11.0",
    "@types/react": "^19.2.14",
    "prettier": "^3.8.1",
    "rollup": "^4.9.0",
    "typescript": "^5.7.0"
  },
  "peerDependencies": {
    "react": "18.x"
  }
}
